server:
  port: 8082

spring:
  application:
    name: example-provider
  main:
    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.163.128:8848

logging:
  level:
    root: info